# Fitness Home Main-20K reproducibility dependencies
#
# The exact versions in the first section were recorded by the
# frozen full-data QLoRA training run.

torch==2.8.0
torchvision==0.23.0
transformers==5.13.0
peft==0.19.1
accelerate==1.14.0
bitsandbytes==0.49.2
datasets==5.0.0

# Required by the retrieval and VLM pipelines.
# Their exact versions were not recorded in the frozen metadata.

numpy
Pillow
sentence-transformers
faiss-cpu


